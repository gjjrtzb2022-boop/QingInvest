import { PlaceholderModule } from "@/components/placeholder-module";

export default function DashboardPage() {
  return (
    <PlaceholderModule
      active="dashboard"
      title="财务仪表盘"
      description="后续接入财报可视化、现金流和估值横向对比。当前页面为模块框架占位。"
    />
  );
}
