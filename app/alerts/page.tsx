import { PlaceholderModule } from "@/components/placeholder-module";

export default function AlertsPage() {
  return (
    <PlaceholderModule
      active="alerts"
      title="价格预警"
      description="后续支持价格、估值、股息率等多维告警。当前页面为模块框架占位。"
    />
  );
}
