# 2026-02-28 详细更新日志

## 更新主题
爬虫内容入库流程打通（answer 首批）+ 文章筛选检索与图片链路升级。

## 背景说明
本轮迭代目标是把“爬虫产物 -> 网站文章库”的流程标准化，减少手工搬运；同时完善文章页筛选、搜索与图片展示，让后续内容扩容可持续执行。

---

## 一、爬虫侧核心信息归档（提炼）

工作目录：`/Users/jianyuanchen/Desktop/Stock_Test/山长知乎文章爬取`

- 目标账号：`https://www.zhihu.com/people/shan-chang-qing-yi`
- 抓取对象：`article / answer / pin`
- 统一字段：`title / link / date / author / content`
- 主要脚本与文件：
  - `zhihu_crawler.py`
  - `crawl_qingyishanchang.sh`
  - `README.md`
  - `.gitignore`
  - `cookie.txt.example`
- 最终产物目录：`/Users/jianyuanchen/Desktop/Stock_Test/山长知乎文章爬取/output_final`
  - 汇总：`summary.json`
  - 结构化原始输出：`all_records.json / all_records.jsonl / all_records.csv`
  - Markdown 主目录：`output_final/records_md/by_type`
- 当前汇总（来自 `summary.json`）：
  - 总计：1648
  - 文章：541
  - 回答：180
  - 想法：927
  - Markdown 导出：1648
  - 图片下载：1772
- 已知说明：
  - 官网显示与导出数量存在差异（可能由重复槽位/展示口径引起）。
  - 高频复查会触发接口 403，需间隔执行或更新 cookie。

---

## 二、网站侧：answer 首批同步上线

### 1) 接入目录与批量导入
- 本次导入来源目录：
  - `/Users/jianyuanchen/Desktop/Stock_Test/山长知乎文章爬取/output_final/records_md/by_type/answer/by_category`
- 新增导入脚本：
  - `/Users/jianyuanchen/Desktop/Stock_Test/tools/import-zhihu-records.mjs`
- 新增 npm 命令：
  - `npm run import:zhihu`

### 2) 首轮导入结果（answer）
- 新增文章：180 篇
- 跳过重复：0 篇（首轮）
- 处理图片：46 张
- 幂等复查：再次 dry-run 结果为“计划新增 0 / 跳过 180”

### 3) 元数据与去重规则
- 按 source_url 去重，避免重复入库。
- 自动提取：标题、链接、日期、作者、分类、标签。
- 标签兼容：
  - 有标签：按原记录导入。
  - 无标签：不强行伪造空占位。
- 分类映射：
  - 有明确分类 -> `series/category` 同步该分类。
  - `待整理` -> 落到未分类系列逻辑。

---

## 三、图片与封面同步链路完善

### 1) 封面策略
- 支持将封面源文件放在：
  - `/Users/jianyuanchen/Desktop/Stock_Test/content/covers`
- 自动同步到：
  - `/Users/jianyuanchen/Desktop/Stock_Test/public/covers`
- 当文章 frontmatter 未填写 `cover` 时，支持按“标题同名”自动匹配封面。

### 2) 正文图片策略
- 新增正文图片源目录：
  - `/Users/jianyuanchen/Desktop/Stock_Test/content/images`
- 自动同步到：
  - `/Users/jianyuanchen/Desktop/Stock_Test/public/images`
- 支持 Markdown 中本地图片路径自动规范化与渲染（含中文文件名与 URL 编码场景）。
- 支持从文章源 `.md` 同目录（如 `images/`、`assets/`）识别并同步到站点可访问路径。

### 3) 同步脚本扩展
- 更新脚本：
  - `/Users/jianyuanchen/Desktop/Stock_Test/tools/sync-covers.mjs`
- 同步范围扩展为：
  - `content/covers -> public/covers`
  - `content/images -> public/images`
  - `content/articles` 下正文图片 -> `public/images/articles`

---

## 四、文章列表筛选与交互升级

### 1) 双向联动筛选
- 标签、行业、个股支持：
  - 文章卡片点击筛选
  - 右侧面板点击筛选
  - 同一筛选状态双向同步
- URL 查询参数同步：
  - `tag / industry / stock`

### 2) 快捷清空能力
- 右侧新增：
  - `全部标签`
  - `全部行业`
  - `全部个股`
- 新增“清空全部筛选”按钮（位于右侧筛选区顶部），一次清空：
  - `status / sort / order / q / tag / industry / stock / series / category / page`

### 3) 分组规则修正
- “其他”板块仅保留未分类文章。
- 一旦文章已归入正式系列，不再在“其他”重复出现。

### 4) 空内容展示优化
- 无标签文章自动隐藏标签行，避免空占位。
- 无行业/无个股时保持隐藏逻辑，列表更干净。

---

## 五、搜索与目录跳转体验更新

### 1) 顶部搜索框启用
- 搜索框右侧改为搜索图标按钮（替代旧快捷键提示）。
- 支持两种触发：
  - 点击搜索按钮
  - Enter 回车
- 逻辑：
  - 在 `/articles` 内检索：更新 `q` 参数并重置分页。
  - 在其他页面检索：跳转到 `/articles?q=关键词`。
  - 空关键词：不触发跳转。

### 2) 目录锚点滚动修复
- 调整正文区 `scroll-margin-top`。
- 点击“正文内容”后落点更准确，减少被顶部区域遮挡的问题。

---

## 六、本次结果

- 已完成“爬虫 answer 首批同步上线”，并具备可重复执行的导入命令。
- 已打通“封面 + 正文图片”的自动同步与渲染链路。
- 已完善列表筛选、清空、搜索、目录跳转等核心交互细节。
- 后续可继续按同一流程同步 `article` 与 `pin` 两类内容。

