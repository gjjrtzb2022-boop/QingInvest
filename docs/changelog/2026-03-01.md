# 2026-03-01 详细更新日志

> 补充说明：2026-03-02 的爬虫侧新增/修复与两日合并结论，见 `docs/changelog/2026-03-02.md`。

## 日期修正说明
- 上一次线上公告日期已修正为 **2026-02-28**（此前误写为 2026-03-01）。
- 本文记录的是 **2026-02-28 之后** 的新增迭代。

---

## 一、内容同步链路重构（爬虫 -> 网站）

### 1) 导入目录自动识别（兼容新旧结构）
- 更新脚本：`tools/import-zhihu-records.mjs`
- 自动识别以下输入目录：
  - `山长知乎文章爬取/output_final/01_内容Markdown/records_md/by_type`
  - `山长知乎文章爬取/output_final/records_md/by_type`
- 作用：爬虫目录改版后无需手改参数，导入可直接执行。

### 2) 导入策略从“仅新增”升级为“新增 + 更新”
- 之前：同 `source_url` 的内容直接跳过。
- 现在：同 `source_url` 会进入更新流程，自动覆盖正文/元信息变化。
- 同步保留字段：`status`、`stocks`、已存在封面等用户状态字段尽量保留。
- 作用：爬虫侧修正内容后，站点可一键对齐，不用手工删稿重导。

### 3) 超长文件名与 slug 规则修复
- 对 answer/pin/article 优先使用链接中的稳定 ID 生成 slug：
  - `zhihu-answer-<id>`
  - `zhihu-pin-<id>`
  - `zhihu-article-<id>`
- 兜底标题 slug 增加截断与 hash，规避 `ENAMETOOLONG`。

---

## 二、大规模构建稳定性与列表性能优化

### 1) 构建稳定性
- 更新文件：`lib/articles.ts`
- 将大批量文章解析改为顺序处理，规避高并发读文件触发的句柄上限问题（`ENFILE`/类似风险）。
- 结果：在大规模内容场景下构建可持续完成。

### 2) 列表页响应优化
- 更新文件：`app/articles/page.tsx`、`components/articles/articles-list-client.tsx`、`lib/articles.ts`
- 优化项：
  - 列表页使用轻量数据结构（不把正文全文下发到列表页）
  - 预构建搜索池，减少筛选时重复字符串拼接
  - 列表封面改为懒加载与异步解码
- 结果：列表筛选与滚动卡顿明显下降。

---

## 三、标题与摘要质量提升

### 1) Pin 无标题自动生成（能力保留）
- 对 pin 场景加入“自动标题生成”逻辑，并追加 `（自动生成）` 标记。
- 处理规则：清洗 HTML/噪声后按正文生成可读标题。

### 2) 摘要从“截断原文”升级为“核心提炼”
- 更新文件：`tools/import-zhihu-records.mjs`、`lib/articles.ts`
- 新摘要目标：
  - 不做表层句子拼接
  - 尝试提炼：主题、核心观点、论证展开、传达意图
  - 尽量压制口语噪声与元信息（链接/来源/编辑信息等）
- 当前输出结构：
  - `文章围绕...展开。`
  - `作者的核心观点是：...。`
  - `文中结合...进行了展开说明。`
  - `文章想传达的是：...。`

---

## 四、发布策略调整：先全下架，再仅上线 articles

### 1) 下架与备份
- 执行前先做本地备份：
  - `content/_offline_backup_20260301-190849/articles`
- 下架动作：清空 `content/articles` 线上内容后重建。

### 2) 仅上线 article
- 重新导入 `article` 类型，当前线上保留 article 内容。
- 当前内容计数：
  - 总计：541
  - article：541
  - answer：0
  - pin：0

---

## 五、验证结果（本轮）
- `npm run typecheck`：通过
- `npm run build`：通过
- 导出页面规模：约 551（含列表/详情/其他静态页）

---

## 六、后续建议（待办）
- 继续打磨摘要“观点抽取”质量（减少个别口语句、提升论证句稳定性）。
- 在摘要层引入行业模板（投资/教育/武道/心理）以增强风格统一性。
- 若需要恢复 answer/pin 上线，可按类型逐步回放并做灰度检查。
